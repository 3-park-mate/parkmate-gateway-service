services:
  gateway:
    container_name: gateway_service
    image: ${DOCKER_USERNAME}/gateway:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - backend

networks:
  backend:
    external: true